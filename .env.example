# ZILA TUI Environment Configuration
# Add this file to your local development environment to override the default API endpoint.
# Make sure NOT to commit the actual .env file, only .env.example should be tracked.

API_BASE=https://zila-api.onrender.com/api
